<div class="container mt-4">
    <div class="row">
        <div class="col-md-4">
            <button type="button" class="btn btn-primary">Học ngay</button>
        </div>
        <div class="col-md-8">
            <h1>{{cource.name}}</h1>
            <iframe width="560" height="315" src="https://www.youtube.com/embed/{{cource.videoId}}" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            <p>{{cource.description}}</p>
        </div>
    </div>
</div>